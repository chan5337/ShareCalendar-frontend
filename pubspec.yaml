name: sharecalendar_app  # 첫 번째 파일의 프로젝트 이름
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.4  # 첫 번째 파일에 맞는 SDK 버전으로 수정

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.19.0  # 추가된 부분
  font_awesome_flutter: ^10.0.0  # 추가된 부분
  flutter_localizations:  # 추가된 부분
    sdk: flutter
  cupertino_icons: ^1.0.8  # 첫 번째 파일에 포함된 패키지

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  # 추가된 자산 및 폰트 관련 항목은 주석처리된 예시
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
